version: 1.0
runtime: nodejs22

build:
  commands:
    pre-build:
      - npm ci
    build:
      - npm run build

run:
command: npx serve -s build -l 8080
network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production
